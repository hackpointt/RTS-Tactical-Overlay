# 简介 
一个用于在RTS游戏中辅助玩家进行多线操作的工具

## 核心功能
辅助玩家在RTS游戏中进行“时间分片循环”操作。

例子：比如在《地狱之门》游戏中，玩家决定同时进攻A阵地。
此时，玩家需要兼顾操作：
1. 突击步兵小队（编号 1）
2. 火力支援装甲部队（编号 2）
3. 预备队（包括医疗兵，编号 3）
4. 支援部队（炮兵等，编号 4）
5. 已占领的B阵地的防御部队（编号 5）
6. 指挥所（到达的增援部队在此集结，需要及时命令它们赶往前线 编号 6）

该工具可以自动定时在 1-6 小队中切换（通过使用定位小队的快捷键，如数字键 1 对应编号 1，工具通过模拟双击数字键 1 切换到该小队）。
切换频率可由用户自行定义，比如一个示例编排：
切到小队 1 - 3s      // 主要操作对象
切到小队 2 - 3s      // 主要操作对象
切到小队 3 - 0.5s    // 大部分情况下仅需看一眼
切到小队 4 - 2s      // 次要操作对象
切到小队 5 - 0.5s    // 仅需看一眼
切到小队 6 - 0.5s    // 仅需看一眼

这个编排包含了 6 个阶段，每个阶段按照设置周期，在持续时间结束后自动切换到下个阶段。

## 特性
 1. 支持win10 及以上
 2. 支持用户自定义编排（json文件）
 3. 支持用户在多个编排中切换
 4. 支持用户在某个阶段中暂停切换流程（以应对突发的、需要长时间专注操作某个小队的情况）
 5. 工具的主界面为一个贴紧屏幕上边缘，居中的窄条，类似一个带节点的进度条。每个节点代表一个阶段。若用户定义的阶段过多，难以一屏显示，则以一种滑动窗口的方式动态展示。
 6. 当用户在某个阶段暂停时，进度条展示冻结动画
 7. 进度条的节点之间的距离代表节点之间的间隔时长，用户可以随时拖动进行实时更改（比如n1 -2s- n2 -0.5s- n3），当用户拖动n2 时，修改n1-n2 的间隔，同时保持n2-n3 的间隔不变。
